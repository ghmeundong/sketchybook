{
  "name": "sketchybook-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --env-file ../.env --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "dotenv": "^16.1.4",
    "wrangler": "^4.0.0"
  }
}
